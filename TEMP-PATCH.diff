*** Begin Patch
*** Update File: pages/LeaderboardPage.tsx
@@
-import React from 'react';
-import { useAppContext } from '../context/AppContext';
+import React from 'react';
+import { useAppContext } from '../context/AppContext';
@@
-    const { leaderboard, player } = useAppContext();
+    const { leaderboard, player, isLoadingLeaderboard } = useAppContext();
@@
-            <div className="bg-gray-800 rounded-lg p-8 shadow-xl overflow-x-auto">
+            <div className="bg-gray-800 rounded-lg p-4 sm:p-8 shadow-xl overflow-x-auto">
                 <table className="w-full text-left min-w-[400px]">
                     <thead className="border-b-2 border-gray-600">
                         <tr>
-                            <th className="p-3 text-lg">Rank</th>
-                            <th className="p-3 text-lg">Name</th>
-                            <th className="p-3 text-lg">Score</th>
+                            <th className="p-3 text-lg">Rank</th>
+                            <th className="p-3 text-lg">Player</th>
+                            <th className="p-3 text-lg">Score</th>
                         </tr>
                     </thead>
                     <tbody>
-                        {leaderboard.length > 0 ? (
-                            leaderboard.map((p, index) => {
-                                const isCurrentUser = p.name === player?.name;
-                                return (
-                                    <tr key={index} className={`${isCurrentUser ? 'bg-cyan-900/50 font-bold' : 'border-b border-gray-700 hover:bg-gray-700/50'}`}>
-                                        <td className="p-3">{index + 1}</td>
-                                        <td className="p-3">{p.name} {isCurrentUser ? '(You)' : ''}</td>
-                                        <td className="p-3">{p.score}</td>
-                                    </tr>
-                                );
-                            })
-                        ) : (
-                            <tr>
-                                <td colSpan={3} className="p-6 text-center text-gray-400">
-                                    The leaderboard is still empty. Be the first to play a game and get on the board!
-                                </td>
-                            </tr>
-                        )}
+                        {isLoadingLeaderboard ? (
+                            [...Array(5)].map((_, i) => (
+                                <tr key={i} className="border-b border-gray-700">
+                                    <td className="p-3"><div className="w-6 h-4 bg-gray-700 animate-pulse rounded" /></td>
+                                    <td className="p-3">
+                                        <div className="flex items-center gap-3">
+                                            <div className="w-8 h-8 rounded-full bg-gray-700 animate-pulse" />
+                                            <div className="h-4 w-40 bg-gray-700 animate-pulse rounded" />
+                                        </div>
+                                    </td>
+                                    <td className="p-3"><div className="h-4 w-12 bg-gray-700 animate-pulse rounded" /></td>
+                                </tr>
+                            ))
+                        ) : (
+                            leaderboard.length > 0 ? (
+                                leaderboard.map((p, index) => {
+                                    const isCurrentUser = p.name === player?.name;
+                                    const initials = p.name.split(/\s+/).slice(0,2).map(s => s[0]?.toUpperCase()||'').join('');
+                                    const hue = (p.name.charCodeAt(0) + p.name.length * 17) % 360;
+                                    return (
+                                        <tr key={index} className={`${isCurrentUser ? 'bg-cyan-900/50 font-bold' : 'border-b border-gray-700 hover:bg-gray-700/50 transition-colors'} focus-within:ring-2 ring-cyan-600`}>
+                                            <td className="p-3 align-middle">{index + 1}</td>
+                                            <td className="p-3 align-middle">
+                                                <div className="flex items-center gap-3">
+                                                    <div className="w-8 h-8 rounded-full text-xs font-bold grid place-items-center" style={{ backgroundColor: `hsl(${hue} 60% 30%)` }} aria-hidden>
+                                                        {initials}
+                                                    </div>
+                                                    <div className="flex items-center gap-2">
+                                                        <span>{p.name} {isCurrentUser ? '(You)' : ''}</span>
+                                                        <span className="text-[10px] uppercase tracking-wide bg-cyan-700/30 text-cyan-300 px-2 py-0.5 rounded">GB</span>
+                                                    </div>
+                                                </div>
+                                            </td>
+                                            <td className="p-3 align-middle">{p.score}</td>
+                                        </tr>
+                                    );
+                                })
+                            ) : (
+                                <tr>
+                                    <td colSpan={3} className="p-6 text-center text-gray-400">
+                                        The leaderboard is still empty. Be the first to play a game and get on the board!
+                                    </td>
+                                </tr>
+                            )
+                        )}
                     </tbody>
                 </table>
             </div>
         </div>
     );
 }
*** End Patch